# 并行推进策略

## 总体思路：3条并行线 + 最后整合

```
               ┌── 采集增强线 ──────────────────┐
               │ ● Playwright 增强采集器          │
               │ ● 全量31省运行 → 数据入库        │
               │ ● 每省进度记录 + 错误处理         │
               └──────────┬──────────────────────┘
                         │
  ┌── 搜索API线 ────────┼─────────────────────┐
  │                     │                     │
  │ ● ES search API    │                     │
  │ ● /api/search      │ ← 数据流入ES         │
  │ ● /api/report/{id} │                     │
  │ ● 高亮/分页/过滤   │                     │
  └──────────┬──────────┘                    │
             │                               │
             └──── 整合线 ───────────────────┘
             │
  ┌── 定时线 ───────────────────────────────┐
  │ ● cron 每天03:00                        │
  │ ● 先增量检查 → 再全量                   │
  │ ● 飞书通知结果                           │
  └─────────────────────────────────────────┘
```

## 依赖关系
- 搜索API 不依赖采集完成（可先写代码再接数据）
- 定时任务依赖采集器就绪
- 全量采集和搜索API可完全并行

## 启动顺序
1. [并行] 增强采集器 + 搜索API代码
2. [并行] 全量采集启动 + 搜索API部署
3. [最后] cron 定时任务配置
